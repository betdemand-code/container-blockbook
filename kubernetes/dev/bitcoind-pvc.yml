apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datapvc-bitcoind
  namespace: dev
spec: 
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources: 
    requests:
      storage: 50Gi